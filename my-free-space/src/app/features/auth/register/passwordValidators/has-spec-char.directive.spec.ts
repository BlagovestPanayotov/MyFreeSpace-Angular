import { HasSpecCharDirective } from './has-spec-char.directive';

describe('HasSpecCharCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new HasSpecCharDirective();
    expect(directive).toBeTruthy();
  });
});
